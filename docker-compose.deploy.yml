version: "3"
services:
  frontend:
    image: veikkoni/lahjarinki/frontend:${LAHJARINKI_VERSION}
    build: ./frontend/
    networks:
      - lahjarinki-internal
    env_file:
      - .env
    ports:
      - "${LAHJARINKI_PORT}:80"

  backend:
    image: lahjarinki/backend:${LAHJARINKI_VERSION}
    build: ./backend/
    networks:
      - lahjarinki-internal
    env_file:
      - .env
